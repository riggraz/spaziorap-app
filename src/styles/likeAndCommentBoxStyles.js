import {StyleSheet} from 'react-native';

const likeAndCommentBoxStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});

export default likeAndCommentBoxStyles;