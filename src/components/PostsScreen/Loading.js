import React from 'react';
import {
  View,
  Text,
  Image,
  StyleSheet,
} from 'react-native';

const Loading = () =>
    <Image
      source={require('../../../assets/images/loading.gif')}
    />

export default Loading;